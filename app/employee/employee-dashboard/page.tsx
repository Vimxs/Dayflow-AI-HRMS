/**
 * Employee Dashboard — stub placeholder
 * Full implementation: T2.1
 */
export default function EmployeeDashboardPage() {
  return (
    <main style={{ display: "flex", minHeight: "100vh", alignItems: "center", justifyContent: "center" }}>
      <div className="glass-card" style={{ padding: "2rem", textAlign: "center" }}>
        <h1 style={{ color: "var(--color-primary)", marginBottom: "0.5rem" }}>Employee Dashboard</h1>
        <p className="caption">Built in Phase 2 (T2.1)</p>
      </div>
    </main>
  );
}
