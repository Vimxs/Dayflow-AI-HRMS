/**
 * Sign In page — stub placeholder
 * Full implementation: T1.4
 */
export default function SignInPage() {
  return (
    <main style={{ display: "flex", minHeight: "100vh", alignItems: "center", justifyContent: "center" }}>
      <div className="glass-card" style={{ padding: "2rem", textAlign: "center" }}>
        <h1 style={{ color: "var(--color-primary)", marginBottom: "0.5rem" }}>Sign In</h1>
        <p className="caption">Authentication UI built in Phase 1 (T1.4)</p>
      </div>
    </main>
  );
}
